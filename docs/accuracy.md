# Measured resolver accuracy (B2 oracle differential)

> **Generated by `node oracle/harness.ts` — do not edit by hand.**
> Last run: 2026-07-18T20:11:04.383Z

Per [ADR-0055](../adr/0055-universal-repo-cartography-computed-not-generated.md) the product refuses scalar
accuracy claims; what it publishes instead is this **measured differential**: the shipped resolver's
resolved-static import edges compared against independent oracle tools on a pinned-SHA corpus.
"Truth" for TS/JS is a 2-of-3 vote (dependency-cruiser, madge, the TypeScript compiler over the real
filesystem — the tsc oracle shares the typescript library with the shipped resolver but uses independent
extraction and host; the two fully-independent tools bound that bias). Python is a single-oracle
differential vs grimp, disclosed as such. Disagreements are evidence for triage, not noise:
see `oracle/results/latest.json`.

| corpus | language | precision | recall | ours | truth | oracle edge counts | time |
|---|---|---|---|---|---|---|---|
| expressjs/express@ae6dd37 | js | 100.00% | 100.00% | 153 | 153 | depcruise: 153 · madge: 153 · tsc: 153 | 1.4s |
| vuejs/core@fa2885d | ts | 99.33% | 100.00% | 1789 | 1777 | depcruise: 1132 · madge: 1776 · tsc: 1789 | 9.1s |
| openai/swarm@6af0b4c | python | 85.71% | 100.00% | 21 | 18 | grimp: 18 | 0.5s |

**Fact class measured:** intra-repo file→file static import edges (`kind: import`,
`resolution: resolved-static`), endpoints restricted to files the pipeline analyzed — so
hygiene differences surface as disagreements, never as silent skew. Reference edges, external
edges, and unresolved-dynamic edges are OUT of scope for this differential (they carry their
own tiers and disclosures in the IR).

**CI gate:** `node oracle/harness.ts --assert` fails when any corpus entry drops below the
floors in `oracle/corpus.json` (`thresholds`). Raise floors deliberately after triaging a run;
never lower them silently.

## Thresholds in force

| corpus | min precision | min recall |
|---|---|---|
| express | 99.0% | 99.0% |
| vue-core | 98.0% | 99.0% |
| swarm | 85.0% | 99.0% |

## Triage record (per pinned SHA — from oracle/corpus.json)

- **express** (expressjs/express@ae6dd37): Measured 2026-07-18: P 100.00% / R 100.00%, 153 edges, zero disagreements.
- **vue-core** (vuejs/core@fa2885d): Measured 2026-07-18: P 99.33% / R 100.00%, 1789 edges. First run caught a real B1 bug (bare-dot `import from '.'` — trailing-slash directoryExists probe; fixed + regression-tested same day; recall 99.83%→100%). The 12 remaining oursOnly are one class: shim .js files (packages/vue/compiler-sfc/*, jsx-runtime/*, server-renderer/*) importing '@vue/x' — we + the tsc oracle apply the repo's own tsconfig paths (→ packages/x/src/index.ts); dependency-cruiser + madge resolve by package semantics instead. A 2-vs-2 modeling split, not an invention: the target file is real and the mapping is the repo's own declared config.
- **swarm** (openai/swarm@6af0b4c): Measured 2026-07-18: P 85.71% / R 100.00% within grimp's model scope (importable package modules); 53 our-edges outside that scope (script files without __init__ chains) spot-verified real and disclosed, not scored. The 3 in-scope oursOnly (examples/airline, examples/personal_shopper) are script-execution sys.path edges (`from configs.tools import *`, `import database`) — real when the file runs as a script (exactly how these examples run), invisible to grimp's package-import model. Our IR discloses script-execution semantics as an assumption; the measured number is published unadjusted.
